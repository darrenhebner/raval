{
  "name": "streamweaver-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "shipit": "pnpm run build && wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc -b",
    "db:reset:local": "wrangler d1 execute streamweaver-demo-db --local --file=./schema.sql",
    "db:reset:prod": "wrangler d1 execute streamweaver-demo-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "@remix-run/fetch-router": "^0.16.0",
    "fast-xml-parser": "^5.3.4",
    "musicbrainz-api": "^1.0.0",
    "streamweaver": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "wrangler": "catalog:"
  }
}
